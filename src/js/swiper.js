const swiper = new Swiper('.swiper', {
    slidesPerView: "auto",
    loop: true,
    centeredSlides: true,
    speed: 8000,
    allowTouchMove: false,
    disableOnInteraction: false,
    autoplay: {
        delay: 1,
    }
});